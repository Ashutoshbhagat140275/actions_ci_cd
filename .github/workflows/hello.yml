name: Hello CI

on:
 push:

jobs:
   say-hello:
     runs-on: ubuntu-latest
     steps:
       - name: Greet
         run: echo "Hello from Github Actions"

       - name:  time
         run: date

       - name: tell the directory
         run: pwd
     
